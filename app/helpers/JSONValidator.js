
/**
 * Validate a JSON array to report on errors and raise concerns on data validity
 *      - check for duplicate keys
 *      - check for keys with no copy
 *      - spell-check?
 * @param {Array} jsonArray
 * @return {Array} jsonArray - containing keys with no value and duplicate keys
 */
module.exports.validate = function (callback) {

};

/**
 * Compare Sources
 * @param {String} fileName
 * @return {Array} jsonArray - status result
 */
module.exports.compare = function (callback) {

};

/**
 * Check JSON for duplicate keys
 *
 */
function checkForDoubleKeys (callback) {

};

/**
 * Check JSON for missing copy
 *
 */
function checkForBlankEntries (callback) {

};

/**
 * Spellcheck JSON
 *
 */
function spellcheck (callback) {

}



